---
description: Copy this session's name to the macOS clipboard
allowed-tools: Bash(~/.claude/commands/copy-session-name.sh)
---

!`~/.claude/commands/copy-session-name.sh`

The command above has already run and its output is inlined here. Report that result
in one short line: the copied name, or the error if it failed. Run nothing else.
